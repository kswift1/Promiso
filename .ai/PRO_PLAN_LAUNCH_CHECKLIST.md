# Pro Plan Launch Checklist

출시 전 Pro 플랜 관련 구현 상태를 현재 코드 기준으로 정리한 문서입니다.

## 상태 기준

- `완료`: 코드상 구현 확인
- `부분 구현`: 핵심은 있으나 체크리스트 문구와 UX 또는 범위가 다름
- `미구현`: 현재 코드/문서 기준 없음
- `제외`: 현재 범위에서 제외
- `수동 QA`: 코드만으로는 확인 불가, 실제 기기/샌드박스 검증 필요

## 1. 미구독자 → 설정 탭 구매 진입

| 항목 | 상태 | 메모 |
|------|------|------|
| 설정 탭에서 Promiso Pro 메뉴 노출 | 제외 | 원문 체크리스트에서 제외 처리됨 |
| 현재 상태 표시 (Free / Pro 활성화됨) | 부분 구현 | Pro는 활성 플랜/관리 액션 표시, Free는 명시적 상태 라벨 없음 |
| Pro 기능 간단 요약 표시 | 제외 | 원문 체크리스트에서 제외 처리됨 |
| “Pro 알아보기” 버튼 → 페이월 연결 | 제외 | 원문 체크리스트에서 제외 처리됨 |
| 이미 구독 중이면 구독 관리 버튼 표시 | 완료 | Pro 사용자는 관리 화면으로 진입 |
| App Store 구독 관리 화면으로 이동 가능 | 완료 | `manageSubscriptionsSheet` 사용 |

## 2. 페이월

| 항목 | 상태 | 메모 |
|------|------|------|
| Pro 사용자일 경우 페이월 진입 차단 | 부분 구현 | 설정 탭 진입은 관리 화면으로 우회되지만, Feature 기본 상태 진입 시 비동기 상태 갱신 전 paywall 분기 가능 |
| StoreKit 상품 로딩 실패 시 에러 UI 표시 | 부분 구현 | alert는 있으나 본문 retry/empty-state 없음 |
| Intro Offer / 무료 체험 조건 체크 | 완료 | `checkIntroOfferEligibility()` 사용 |
| 구매 내역 복원 버튼 정상 작동 | 완료 | `restoreWithReceipt` + 서버 검증 경로 존재 |
| 구매 진행 중 로딩 상태 표시 및 버튼 비활성화 | 완료 | overlay + dismiss/CTA/restore 비활성화 |
| 선택된 요금제에 따라 CTA 문구 변경 | 완료 | 무료 체험 / 평생 / 일반 구독별 CTA 다름 |

## 3. Pro 기능 게이팅

| 항목 | 상태 | 메모 |
|------|------|------|
| 출발시간 계산 → 미구독자 클릭 시 페이월 | 부분 구현 | 현재는 무료 사용자에게 CTA 자체를 숨김 |
| 일정 충돌 감지 → 토글 비활성 + 페이월 | 완료 | 설정 화면에서 비활성 + 탭 시 Pro 진입 |
| 날씨 정보 → Blur 처리 + Pro 안내 | 미구현 | 현재는 무료 사용자에게 날씨를 fetch하지 않고 그냥 숨김 |
| AI 브리핑 → 요약만 표시 / 상세 Blur | 완료 | 요약은 보이고 상세는 blur + 업그레이드 CTA |

## 4. 구독 상태 관리

| 항목 | 상태 | 메모 |
|------|------|------|
| 앱 실행 시 restoreSubscription() 수행 | 미구현 | 현재는 `restore`가 아니라 `fetchStatus + unifiedStatusStream` |
| 구독 상태 관리: Free / Active / Expired / Grace Period | 완료 | `SubscriptionStatus` 모델 존재 |
| 구독 상태 변경 시 UI 자동 업데이트 | 완료 | 앱 레벨에서 상태 스트림 구독 |
| 구독 취소 시 현재 결제 기간까지 Pro 유지 | 완료 | expiration date 기준 active 상태 유지 |

## 5. 구독 관련 UI

| 항목 | 상태 | 메모 |
|------|------|------|
| 페이월 하단에 이용약관 링크 | 완료 | 링크 존재 |
| 개인정보처리방침 링크 | 완료 | 링크 존재 |
| 구독 자동 갱신 안내 문구 표시 | 완료 | 선택 상품 기준 동적 문구 표시 |

## 6. UX 개선 (추천)

| 항목 | 상태 | 메모 |
|------|------|------|
| Pro 기능 클릭 시 설명 → 페이월 | 부분 구현 | 브리핑/설정 일부는 해당, 출발/날씨는 일관되지 않음 |
| 페이월 Hero에 문제 공감 카피 유지 | 완료 | hero + problem/solution 카드 유지 |
| 기능 카드에 실제 UI 미리보기 유지 | 완료 | 출발/충돌/브리핑 preview 카드 존재 |
| Pricing 카드에서 연간 플랜 강조 | 완료 | 연간 기본 선택 + 강조 배지 있음 |

## 7. Analytics (추천)

| 항목 | 상태 | 메모 |
|------|------|------|
| paywall_open | 완료 | Paywall 노출 시 Firebase Analytics 로깅 |
| paywall_purchase | 완료 | 구매 CTA 탭 시 Firebase Analytics 로깅 |
| paywall_restore | 완료 | 복원 CTA 탭 시 Firebase Analytics 로깅 |
| paywall_close | 완료 | Paywall 종료 시 Firebase Analytics 로깅 |

## 출시 전 최종 확인

| 항목 | 상태 | 메모 |
|------|------|------|
| 페이월 진입 → 구매 → 구독 활성화 전체 플로우 테스트 | 수동 QA | 실제 샌드박스 또는 dev 빌드 확인 필요 |
| Sandbox 계정으로 구독 / 취소 / 복원 테스트 | 수동 QA | 코드상 경로는 존재하나 실기기 검증 필요 |
| 다크모드 UI 확인 | 수동 QA | 수동 확인 필요 |
| 네트워크 실패 상황 테스트 | 부분 구현 | 구매/복원/상품 로딩 실패 처리 코드는 있으나 시나리오 기반 QA 문서화 필요 |

## 남은 작업 우선순위

1. 날씨 / 출발시간 게이팅 UX를 체크리스트 기준으로 통일
2. 설정 탭에서 Free 상태를 명시적으로 보여줄지 결정
3. 상품 로딩 실패 시 retry 가능한 에러 UI 추가 여부 결정
4. 앱 시작 시 `restore`가 정말 필요한지, 현재 `fetchStatus` 기반으로 충분한지 정책 결정
